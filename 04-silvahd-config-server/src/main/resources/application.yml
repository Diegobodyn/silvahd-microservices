server:
  port: 8888
spring:
  application:
    name: silvahd-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Diegobodyn/silvahd-config-server.git
          default-label: main
          search-paths:
          - 'greeting-service*'